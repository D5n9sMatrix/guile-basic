/'

   The document series guile freebasic base of lledcout
   notion linear position to make sutable measure level
   about production of nuclear documentation guile gnu
   hack to production in level measure logical ordering
   nuclear money to stability of humanity in life easy
   to jobs nuclear production to all humanity.

'/

declare operator = (byref lhs as integer, byref rhs as integer)

''
'' Dynamic values nuclear money production to all humanity
'' life easy to all humanity.
''

dim as integer nuclear_money_production = 1024 * 8
dim as byte ptr money = allocate( nuclear_money_production )


''
'' Sometimes to kk wiki buffer ...
''

for i as integer = 0 to nuclear_money_production * 100
    money[i] = 123
next


''
'' Free allocate money nuclear to all humanity
''

allocate(money)

''
'' Policy Millitaty look view all elements
''

type Policy_Millitary
     view_elements_small_one as integer
     view_elements_small_two as integer
     view_elements_small_thd as integer 
end type

''
'' Policy Millitary Apples Mulyiples nuclear money
''

dim apples_look_key_one as view_elements_small_one ptr
dim apples_look_key_two as view_elements_small_two ptr
dim apples_look_key_thd as view_elements_small_thd ptr

''
'' Policy Millitary look dog talk level measure to view elements
''

apples_look_key_one = allocate(sizeof(Policy_Millitary))
apples_look_key_two = allocate(sizeof(Policy_Millitary))
apples_look_key_thd = allocate(sizeof(Policy_Millitary))

''
'' Policy Millitary look the elements packages production
''

apples_look_key_one->view_elements_small_one = 1234
apples_look_key_two->view_elements_small_two = 5678
apples_look_key_thd->view_elements_small_thd = 9102

''
'' Policy Millitary look view output elements
''
print apples_look_key_one->view_elements_small_one
print apples_look_key_two->view_elements_small_two
print apples_look_key_thd->view_elements_small_thd

''
'' Policy Millitary deallocate view elements 
''

deallocate(apples_look_key_one)
deallocate(apples_look_key_two)
deallocate(apples_look_key_thd)

sleep 

end 